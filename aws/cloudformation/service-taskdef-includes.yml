Environment:
  - Name: LOG_LEVEL
    Value: DEBUG
#Secrets:
#  - Name: DATABASE_HOST
#    ValueFrom: 
#      Fn::Sub: "arn:aws:ssm:${AWS::Region}:${AWS::AccountId}:parameter/${EnvironmentName}/DbHost"
#  - Name: DATABASE_USERNAME
#    ValueFrom: 
#      Fn::Sub: "arn:aws:ssm:${AWS::Region}:${AWS::AccountId}:parameter/${EnvironmentName}/${ServiceName}/AppUser"
#  - Name: DATABASE_PASSWORD
#    ValueFrom: 
#      Fn::Sub: "arn:aws:ssm:${AWS::Region}:${AWS::AccountId}:parameter/${EnvironmentName}/${ServiceName}/AppPassword"
