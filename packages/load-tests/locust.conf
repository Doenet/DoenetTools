# Default Locust configuration for Doenet load tests.
# Run all personas with: locust --config packages/load-tests/locust.conf
# Or simply: locust   (when run from the packages/load-tests/ directory)

locustfile = guest.py,student_anonymous.py,student.py,instructor.py,author.py
host = https://dev3.doenet.org
users = 100
spawn-rate = 10