{
  "name": "@doenet-tools/shared",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "Shared utilities for DoenetTools",
  "scripts": {
    "format": "prettier . --write",
    "format:check": "prettier . --check",
    "lint": "eslint --fix",
    "lint:check": "eslint"
  },
  "dependencies": {
    "ipfs-only-hash": "^4.0.0"
  }
}
